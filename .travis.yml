language: node_js
node:
  - 16

before_install: npm install -g pnpm

# install any dependencies required
install: pnpm install

# TODO: unit testing
before_script: echo "unit test placeholder"

# run the build script
script: pnpm run build
